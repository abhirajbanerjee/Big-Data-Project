# Big Data Project — Quickstart (local Docker)

## Files added
- Dockerfile
- docker-compose.yml
- requirements.txt
- .env.example
- src/ (original project files)

## Run locally (steps)
1. Unzip project and `cd` into project root.
2. Copy .env example:
   ```bash
   cp .env.example .env
